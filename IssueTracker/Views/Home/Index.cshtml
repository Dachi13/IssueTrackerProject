@model List<Ticket>

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h2>Tickets</h2>
    <table class="table">
        <thead>
        <tr>
            <th>Title</th>
            <th>Priority</th>
            <th>Status</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var ticket in Model)
        {
            <tr>
                <td>@ticket.Title</td>
                <td>@ticket.Priority</td>
                <td>@ticket.Status</td>
            </tr>
        }
        </tbody>
    </table>
</div>